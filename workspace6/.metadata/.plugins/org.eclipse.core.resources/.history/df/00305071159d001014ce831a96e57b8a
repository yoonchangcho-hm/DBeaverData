use testdb;

-- 회원 테이블
CREATE TABLE user (
  userId BIGINT AUTO_INCREMENT PRIMARY KEY,
  useremail VARCHAR(255) NOT NULL UNIQUE,
  username VARCHAR(100) NOT NULL,
  password VARCHAR(255) NOT NULL,
  createdAt DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 게시글 테이블
CREATE TABLE post (
  postId BIGINT AUTO_INCREMENT PRIMARY KEY,
  title VARCHAR(255) NOT NULL,
  content TEXT NOT NULL,
  islive BOOLEAN NOT NULL DEFAULT TRUE,
  userId BIGINT NOT NULL,
  createdAt DATETIME DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT fk_post_user FOREIGN KEY (userId) REFERENCES user(userId)
);

INSERT INTO user (useremail, username, password) VALUES
('alice@example.com', 'Alice', 'pw1'),
('bob@example.com', 'Bob', 'pw2'),
('charlie@example.com', 'Charlie', 'pw3'),
('diana@example.com', 'Diana', 'pw4'),
('eric@example.com', 'Eric', 'pw5'),
('fiona@example.com', 'Fiona', 'pw6'),
('george@example.com', 'George', 'pw7'),
('hana@example.com', 'Hana', 'pw8'),
('ian@example.com', 'Ian', 'pw9'),
('julia@example.com', 'Julia', 'pw10');

INSERT INTO post (title, content, userId) VALUES
('첫 번째 글', 'Alice의 첫 글입니다.', 1),
('두 번째 글', 'Bob의 블로그 글입니다.', 2),
('세 번째 글', 'Charlie의 첫 글.', 3),
('네 번째 글', 'Diana의 블로그 시작!', 4),
('다섯 번째 글', 'Eric이 쓴 글.', 5),
('여섯 번째 글', 'Fiona의 일상 기록.', 6),
('일곱 번째 글', 'George의 여행 후기.', 7),
('여덟 번째 글', 'Hana의 개발 블로그.', 8),
('아홉 번째 글', 'Ian의 노트.', 9),
('열 번째 글', 'Julia의 일상 기록.', 10);